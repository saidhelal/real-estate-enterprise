import { LeadershipWorkspace } from "@/pages/leadership-workspace";

/** The executive director's workspace. Operations: assignments first, then decisions. */
export default function ExecutiveDirectorPage() {
  return <LeadershipWorkspace role="executive_director" />;
}
